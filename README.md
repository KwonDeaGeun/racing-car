# Highway Time Trial

A single-player 3D time-trial driving game built with React Three Fiber and Rapier.

## Development

Install dependencies:

```bash
pnpm install
```

Start the Vite development server:

```bash
pnpm dev
```

Create a production build:

```bash
pnpm build
```
